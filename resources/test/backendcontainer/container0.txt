# Configuration of Container 0
# container attached to an existing platform (main container) 
container=true

# container name
container-name=Container-0

# host that the main container is running
#host=bzafiris-PC
host=localhost

# port that the main container is running
port=1099
# show the rma agent
ShowGUI=false

# host name where this container is going to run
#local-host=bzafiris-PC
local-host=localhost

# port where this container can be contacted, defaults to 1099
local-port=1124

# port for the http mtp service
HTTPPort=7779

# other options related to inter-platform mobility 
AgentsPath=jars


